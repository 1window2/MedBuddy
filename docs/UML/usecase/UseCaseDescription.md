### Use Case Description: UC-01 처방전 이미지 입력 및 인식

| Actor Action | System Response |
| --- | --- |
|  | 1. 처방전 촬영 또는 사진첩 이미지 선택 메뉴를 화면에 출력한다. |
| 2. 사용자가 처방전 입력 방법을 선택한다. |  |
|  | 3. 선택한 입력 방법에 해당하는 촬영 화면 또는 이미지 선택 화면을 출력한다. |
| 4. 사용자가 처방전 이미지를 촬영하거나 선택한다. |  |
|  | 5. 기기에서 문자 인식과 개인정보 masking을 수행하고, 비식별 text를 분석하여 인식된 복약 일정을 화면에 출력한다. |

Extensions  
Step 2 이후, 사용자가 카메라 촬영을 선택하여 가이드에 따라 처방전을 촬영한다. (UC-21)  
Step 5 이후, 사용자가 잘못 인식된 값을 발견하면 해당 값을 수정한다. (UC-17)

---

### Use Case Description: UC-02 처방 약 분석 결과 확인

| Actor Action | System Response |
| --- | --- |
| 1. 사용자가 검토한 처방 정보의 분석 시작 버튼을 선택한다. |  |
|  | 2. 약명을 검증하고 의약품 기본정보·허가정보·복약 주의사항을 조회한다. |
|  | 3. 분석에 성공한 약, 약명 불일치 약과 일시적 조회 실패 약을 원래 처방 행 기준으로 구분한다. |
|  | 4. 일부 약만 분석된 경우 성공 결과는 보존하고, 확인되지 않은 약만 수정·재시도할 수 있도록 검토 화면에 출력한다. |
| 5. 사용자가 확인되지 않은 약명을 수정하여 다시 분석하거나 검토 화면으로 돌아간다. |  |
|  | 6. 재시도 대상만 다시 조회하며, 전체가 일시적으로 실패한 경우에도 촬영 이미지와 OCR 수정 결과를 유지한 채 재시도 또는 검토 복귀 기능을 출력한다. |

Extensions  
Step 4 이후, 사용자가 분석된 약을 개별 또는 전체 저장한다. (UC-18)

---

### Use Case Description: UC-03U 홈 오늘 복약 요약 확인

| Actor Action | System Response |
| --- | --- |
|  | 1. 홈 화면에 오늘 복약 완료 수, 전체 복약 수와 남은 시간대 정보를 요약하여 출력한다. |

---

### Use Case Description: UC-03P 환자 오늘 복약 일정 확인

| Actor Action | System Response |
| --- | --- |
|  | 1. 오늘 복용해야 하는 약 목록을 시간대별로 분류하고 복약 완료 여부와 알림 설정 상태를 화면에 출력한다. |

Extensions  
Step 1 이후, 환자가 시간대별 복약 완료 또는 완료 취소를 선택한다. (UC-08)  
Step 1 이후, 환자가 특정 약을 선택하여 상세 정보를 확인한다. (UC-09)  
Step 1 이후, 환자가 건강 추천 버튼을 선택하여 복약 기반 추천을 확인한다. (UC-10)  
Step 1 이후, 환자가 알림 아이콘을 선택하여 복약 알림을 설정한다. (UC-12)

---

### Use Case Description: UC-04P 환자 저장 약 관리

| Actor Action | System Response |
| --- | --- |
|  | 1. 환자가 저장한 약을 복용 상태와 날짜에 따라 분류하여 화면에 출력한다. |
| 2. 환자가 복용 중·종료·전체 조건이나 정렬 기준을 선택한다. |  |
|  | 3. 선택한 조건에 따라 저장 약 목록을 filtering, sorting 및 grouping하여 다시 출력한다. |

Extensions  
Step 1 이후, 환자가 저장 약을 선택하여 삭제한다. (UC-05)  
Step 1 이후, 환자가 특정 저장 약을 선택하여 상세 정보를 확인한다. (UC-09)

---

### Use Case Description: UC-04C 연결 환자의 복약 현황 확인

| Actor Action | System Response |
| --- | --- |
|  | 1. 보호자와 활성 연결된 환자 목록을 보호자 기기에 저장된 환자별 표시 이름으로 출력한다. 저장된 이름이 없으면 기본 표시 이름을 사용한다. |
| 2. 보호자가 확인할 환자를 선택한다. |  |
|  | 3. 연결 권한을 검증하고 선택한 환자의 기기 표시 이름, 저장 약과 오늘 복약 일정을 읽기 전용으로 출력한다. |

Extensions  
Step 3 이후, 보호자가 특정 약을 선택하여 상세 정보를 확인한다. (UC-09)  
Step 3 이후, 보호자가 시간대별 보호자 알림 정책을 설정한다. (UC-22)

---

### Use Case Description: UC-05 저장 약 삭제

| Actor Action | System Response |
| --- | --- |
| 1. 환자가 단일 약, 날짜 그룹 또는 여러 저장 약을 선택한다. |  |
|  | 2. 선택된 약과 삭제 확인 화면을 출력한다. |
| 3. 환자가 삭제를 승인한다. |  |
|  | 4. 소유권을 검증하고 저장 약과 관련 복약 완료 기록을 삭제한 후 목록을 갱신한다. |

---

### Use Case Description: UC-06 환자-보호자 연결

| Actor Action | System Response |
| --- | --- |
|  | 1. 환자-보호자 연결 상태와 연결 기능을 화면에 출력한다. |
| 2. 환자는 연결 코드 생성을 선택하고, 보호자는 연결 코드 입력을 선택한다. |  |
|  | 3. 환자에게는 15분 동안 유효한 8자리 연결 코드를 출력하고, 보호자에게는 코드 입력 화면을 출력한다. |
| 4. 보호자가 환자에게 전달받은 연결 코드를 입력하고 등록 버튼을 선택한다. |  |
|  | 5. 코드의 유효기간·사용 여부·자기 연결 여부를 검증하고 연결 결과를 화면에 출력한다. |
| 6. 보호자가 연결된 환자의 표시 이름 수정 기능을 선택하고 이름을 입력한다. |  |
|  | 7. 입력값을 정리하여 보호자 기기에 환자별로 저장하고 연결 목록에 반영한다. |

Extensions  
Step 5 이후, 환자 또는 보호자가 활성 연결을 해제한다. (UC-07)

---

### Use Case Description: UC-07 환자-보호자 연결 해제

| Actor Action | System Response |
| --- | --- |
| 1. 환자 또는 보호자가 연결 목록에서 연결 해제를 선택한다. |  |
|  | 2. 해제할 연결 정보와 확인 화면을 출력한다. |
| 3. 사용자가 연결 해제를 승인한다. |  |
|  | 4. 사용자가 연결 당사자인지 검증하고 연결과 관련 보호자 알림 설정을 해제한 후 결과를 출력한다. |
|  | 5. 보호자 기기에서는 해제된 환자의 표시 이름과 알림 중복 방지 정보를 정리하고 연결 목록을 갱신한다. |

---

### Use Case Description: UC-08 시간대별 복약 완료 및 취소

| Actor Action | System Response |
| --- | --- |
| 1. 환자가 특정 약의 복약 완료 버튼 또는 시간대의 전부 체크 버튼을 선택한다. |  |
|  | 2. 개별 선택이면 해당 약만, 전부 체크이면 해당 시간대의 모든 활성 약을 하나의 Transaction에서 완료 처리하고 화면에 반영한다. |
| 3. 환자가 완료된 약의 완료 취소 또는 완료된 시간대의 전부 해제를 선택한다. |  |
|  | 4. 동일한 범위의 완료 기록을 취소하고 미완료 상태를 화면에 반영한다. |

Extensions  
Step 2 이후, 보호자 완료 알림 조건이 충족되면 보호자에게 복약 완료 알림을 전달한다. (UC-13)

---

### Use Case Description: UC-09 약 상세 정보 확인

| Actor Action | System Response |
| --- | --- |
| 1. 사용자가 복약 일정, 저장 약 목록 또는 연결 환자 현황에서 특정 약을 선택한다. |  |
|  | 2. 약 이름, 효능, 복용법, 권장 용량, 주의사항, 상호작용, 부작용과 보관 방법을 화면에 출력한다. |

Extensions  
Step 2 이후, 사용자가 음성 안내 버튼을 선택하여 약 상세 정보를 음성으로 듣는다. (UC-11)

---

### Use Case Description: UC-10 건강 추천 확인

| Actor Action | System Response |
| --- | --- |
| 1. 환자가 오늘 복약 일정 화면에서 건강 추천 버튼을 선택한다. |  |
|  | 2. 환자의 현재 복약 일정과 저장된 추천 cache를 확인한다. |
|  | 3. 필요한 경우 인공지능 서비스에 추천을 요청하고 건강 관리 안내와 주의사항을 화면에 출력한다. |

---

### Use Case Description: UC-11 음성 안내 재생

| Actor Action | System Response |
| --- | --- |
| 1. 사용자가 약 상세 화면에서 음성 안내 버튼을 선택한다. |  |
|  | 2. 약 상세 정보를 읽기 위한 음성 안내 text를 생성하고 기기 음성 합성 기능으로 재생한다. |
| 3. 사용자가 음성 안내 버튼을 다시 선택하거나 상세 화면을 종료한다. |  |
|  | 4. 재생 중인 음성 안내를 중지한다. |

---

### Use Case Description: UC-12 환자 복약 알림 설정

| Actor Action | System Response |
| --- | --- |
| 1. 환자가 홈 빠른 기능의 복약 알림 설정 또는 오늘 복약 일정의 시간대별 알림 기능을 선택한다. |  |
|  | 2. 통합 설정 화면에 아침·점심·저녁·취침 전의 현재 알림 상태와 시간을 출력한다. |
| 3. 환자가 알림 시간을 선택하고 저장 버튼을 누른다. |  |
|  | 4. 시간대별 알림 설정을 저장하고 복약 기간 안에서 다음 14일 동안의 알림을 기기에 예약한다. |
| 5. 환자가 설정된 알림의 해제를 선택한다. |  |
|  | 6. 저장된 알림 설정을 비활성화하고 기기에 예약된 알림을 취소한다. |
|  | 7. Android에서는 주기적인 background 작업이 서버의 현재 설정과 일정을 다시 확인하여 다음 14일 동안의 알림을 보충하고, 일시적 실패 시 재시도한다. |

---

### Use Case Description: UC-13 보호자 복약 완료 알림 수신

| Actor Action | System Response |
| --- | --- |
|  | 1. 연결 환자의 시간대 복약이 새로 모두 완료되면 완료 기록과 중복되지 않는 보호자 알림 요청을 함께 저장한다. |
|  | 2. 복약 완료 응답을 지연하지 않도록 저장된 알림 요청을 background에서 처리한다. |
|  | 3. 활성 연결, 복약 완료 알림 설정과 보호자의 활성 기기를 확인하여 Firebase 인증 mode에서는 FCM Push를 전송한다. |
|  | 4. 전송이 일시적으로 실패하면 안전하게 재시도하고, 반복 실패한 요청은 이후 확인할 수 있도록 보존한다. 인증 비활성 개발 mode에서는 기기 내 상태 감시를 통해 알림을 표시한다. |
| 5. 보호자가 기기에 표시된 복약 완료 알림을 확인한다. |  |
|  | 6. 인증 비활성 개발 mode에서는 보호자 기기에 저장된 환자 표시 이름을 우선 표시하고, 이름이 없으면 내부 환자 hash 대신 일반 연결 환자 문구와 복약 시간대를 표시한다. |

---

### Use Case Description: UC-14 사용자 설정 관리

| Actor Action | System Response |
| --- | --- |
|  | 1. 마스킹된 계정 요약과 복약 및 알림, 화면 및 음성, 실험실, 계정 메뉴를 환경설정 허브에 출력한다. |
| 2. 사용자가 복약 및 알림, 화면 및 음성 또는 실험실 메뉴를 선택한다. |  |
|  | 3. 선택한 설정 화면을 열고 하위 화면 사이를 이동해도 아직 저장하지 않은 값을 유지한다. |
| 4. 사용자가 알림 종류별 허용 여부, 신규 일정 기본 시각, 잠금 화면 공개 범위, 글자 크기, 읽기 속도, 언어, 시간 표시 방식 또는 실험 기능 값을 변경한다. |  |
|  | 5. 변경된 글씨와 언어는 현재 화면에 미리 적용하고 음성 속도는 미리듣기에 반영한다. 기본 시각은 이후 생성되는 일정에만 사용한다. |
| 6. 사용자가 저장 버튼을 선택한다. |  |
|  | 7. 전체 설정을 사용자 범위로 저장하고 알림 허용·잠금 화면 정책을 예약 알림과 보호자·채팅 알림에 반영한 뒤 환경설정 화면을 유지한다. |
|  | 8. 무과금 베타에서는 SMS 다중 인증 UI를 표시하지 않으며, 관련 구현은 기능 flag 뒤에 비활성 상태로 유지한다. |

Extensions  
Step 1 이후, 사용자가 로그아웃 또는 계정 데이터 삭제 기능을 선택한다. (UC-16)

---

### Use Case Description: UC-15 인증 및 세션 생성

| Actor Action | System Response |
| --- | --- |
|  | 1. Firebase와 Firebase App Check를 초기화하는 동안 보안 초기화 상태를 출력한다. |
|  | 2. 무과금 베타에서 Email, Google 및 Guest 인증 방법을 제공하는 로그인 화면을 출력한다. |
| 3. 사용자가 인증 방법을 선택하고 필요한 인증 정보를 입력한다. |  |
|  | 4. Firebase를 통해 입력 정보를 검증하고 필요한 경우 Email 인증 화면을 출력한다. Phone 및 SMS 다중 인증 구현은 기능 flag 뒤에 비활성 상태로 유지한다. |
| 5. 사용자가 전달받은 인증 절차를 완료한다. |  |
|  | 6. Firebase 인증 정보와 App Check Token을 이용하여 사용자 identity와 앱의 무결성을 검증한다. 개발 인증 비활성 mode에서는 App Check 검증을 생략한다. |
|  | 7. 검증된 사용자 식별자로 사용자 계정을 등록하거나 갱신하고 인증 Session을 생성한다. |
|  | 8. 인증 상태에 따라 홈 화면으로 전환한다. 초기화 제한시간 초과 또는 Backend Session 실패 시 각각 보안 초기화 재시도 또는 Session 재시도 기능을 출력한다. |

---

### Use Case Description: UC-16 계정 생명주기 관리

| Actor Action | System Response |
| --- | --- |
|  | 1. 사용자 설정 화면에 로그아웃과 계정 데이터 삭제 기능을 출력한다. |
| 2. 사용자가 로그아웃 또는 계정 데이터 삭제를 선택한다. |  |
|  | 3. 계정 데이터 삭제를 선택한 경우 삭제 범위와 확인 메시지를 출력하고, credential 기반 계정에는 최근 인증 또는 Google 재인증을 요구한다. |
| 4. 사용자가 로그아웃을 실행하거나 계정 데이터 삭제를 승인한다. |  |
|  | 5. 로그아웃 시 background reminder 작업과 예약된 알림을 취소하고, 인증 정보가 유효한 동안 기기 Push token을 해제한 뒤 Firebase Session을 종료한다. Push token 정리가 실패하면 Session을 유지하여 재시도할 수 있게 한다. |
|  | 6. 계정 삭제 시 삭제 진행 상태를 안전하게 기록하고 MedBuddy 데이터를 정리한 뒤 검증된 Firebase identity를 삭제하여 삭제를 완료한다. |
|  | 7. Firebase identity 삭제가 일시적으로 실패하면 삭제 진행 상태와 화면을 유지하여 동일 요청을 안전하게 재시도하고, 성공하면 기기 cache와 인증 Session을 정리한다. |

---

### Use Case Description: UC-16A 계정 데이터 내보내기

| Actor Action | System Response |
| --- | --- |
| 1. 인증된 API Client가 계정 데이터 내보내기 endpoint를 요청한다. |  |
|  | 2. 요청자의 인증 정보와 사용자 식별자를 검증한다. |
|  | 3. `user_hash`, 저장 약, 복약 완료, 환자 알림, 환자-보호자 연결 및 보호자 알림 설정을 수집하여 JSON으로 반환한다. |

---

### Use Case Description: UC-17 문자 인식 결과 수정

| Actor Action | System Response |
| --- | --- |
|  | 1. 개인정보가 masking된 처방 이미지와 인식된 약명·복용 횟수·복용 일수를 검토 화면에 출력한다. |
| 2. 사용자가 잘못 인식된 약명 또는 복약 정보를 수정하거나 OCR에서 누락된 약을 직접 추가한다. |  |
|  | 3. 최초 인식값을 보존하고 변경된 값과 직접 추가된 약을 사용자 검토 정보로 표시한다. |
| 4. 사용자가 수정 결과를 확인하고 분석 시작 버튼을 선택한다. |  |
|  | 5. 수정·추가된 복약 정보를 다음 처방 약 분석 대상으로 전달한다. 상세조회에서 일부 약이 불일치하거나 일시적으로 실패하면 성공한 행을 잠그고 실패한 행만 다시 수정·조회할 수 있는 검토 화면으로 복귀한다. |

---

### Use Case Description: UC-18 분석 약 저장

| Actor Action | System Response |
| --- | --- |
| 1. 사용자가 분석 결과에서 특정 약의 저장 버튼 또는 전체 저장 버튼을 선택한다. |  |
|  | 2. 선택한 약의 상세 정보와 처방에서 인식한 복약 일정, 동일 분석 처방을 구분하는 정보를 저장한다. |
|  | 3. 저장 성공·중복·실패 약의 수를 구분하여 결과를 화면에 출력한다. |
|  | 4. 저장 약 목록과 오늘 복약 일정을 갱신한다. |

---

### Use Case Description: UC-19 처방 변경 비교 확인

| Actor Action | System Response |
| --- | --- |
|  | 1. 처방 약 분석 결과 화면과 처방 변경 비교 loading 상태를 먼저 출력한다. |
|  | 2. 화면 표시를 차단하지 않고 현재 처방과 이전 저장 처방의 비교를 비동기로 요청한다. 이전 처방은 같은 날짜에 저장됐더라도 처방 분석 단위로 구분하며, 구분 정보가 없는 기존 데이터만 날짜 단위로 묶는다. |
|  | 3. 추가된 약, 제외된 약, 복용 정보가 변경된 약과 유지된 약을 구분하여 비교 영역을 갱신한다. |
| 4. 사용자가 갱신된 처방 변경 비교 영역을 확인한다. |  |

---

### Use Case Description: UC-20 낱알 약 후보 식별

| Actor Action | System Response |
| --- | --- |
|  | 1. 낱알 약의 앞면과 뒷면 이미지를 입력할 수 있는 화면을 출력한다. |
| 2. 사용자가 앞면 이미지를 필수로 입력하고 필요한 경우 뒷면 이미지를 추가한다. |  |
|  | 3. 선택한 이미지와 촬영 품질 안내를 화면에 출력한다. |
| 4. 사용자가 후보 찾기 버튼을 선택한다. |  |
|  | 5. 이미지 전처리로 서로 분리된 알약 영역 수를 확인하고, 두 개 이상이면 후보 조회 전에 한 알씩 다시 촬영하도록 안내한다. |
|  | 6. 시각 분석 결과의 감지 개수가 한 개가 아니거나 신뢰할 수 있는 기기 전처리 결과와 다르면 재촬영을 안내한다. |
|  | 7. 단일 알약으로 확인된 이미지에서 색상·모양·각인 특징을 추출하여 의약품 catalog와 비교한다. |
|  | 8. 일치 점수와 함께 가능한 약 후보를 화면에 출력한다. |
| 9. 사용자가 실제 약과 일치한다고 판단한 후보를 선택하고 확인한다. |  |
|  | 10. 선택한 후보를 사용자 확인 결과로 표시한다. |

---

### Use Case Description: UC-21 가이드 기반 처방전 촬영

| Actor Action | System Response |
| --- | --- |
|  | 1. 촬영 화면에서 현재 세로·가로 방향에 맞춘 카메라 영상과 처방전 배치 가이드 영역을 출력한다. |
| 2. 사용자가 처방전을 가이드 영역 안에 맞추고 촬영 버튼을 선택한다. |  |
|  | 3. 영상 frame의 문서 위치와 촬영 품질을 확인한다. |
|  | 4. 화면 가이드 좌표를 촬영 원본 이미지 좌표로 변환하고 가이드 안쪽 영역만 자른다. |
|  | 5. 촬영 방향을 반영해 잘라낸 이미지를 처방전 문자 인식 대상으로 반환하고 앱이 생성한 자르기 전 원본은 제거한다. 촬영 화면 종료 시 세로 방향 정책을 복원한다. |

---

### Use Case Description: UC-22 보호자 미복약 알림 정책 설정 및 감지

| Actor Action | System Response |
| --- | --- |
| 1. 보호자가 연결 환자의 특정 시간대 알림 아이콘을 선택한다. |  |
|  | 2. 알림 해제, 복약 완료 알림, 미복약 마감 알림을 선택할 수 있는 화면을 출력한다. |
| 3. 보호자가 미복약 마감 알림과 마감 시간을 선택하고 저장한다. |  |
|  | 4. 환자와 시간대별 보호자 알림 정책을 저장하고 Background 점검 작업을 등록한다. |
|  | 5. 설정된 마감 시간이 지난 후 해당 복약이 완료되지 않았는지 주기적으로 확인한다. |
|  | 6. 미복약 조건이 충족되면 보호자 기기에 local notification을 출력한다. |
| 7. 보호자가 미복약 알림을 확인한다. |  |

---

### Use Case Description: UC-23 의약품 목록 동기화

| Actor Action | System Response |
| --- | --- |
| 1. 운영 실행 환경에서 동기화할 dataset과 page 범위를 지정하여 동기화 명령을 실행한다. |  |
|  | 2. 식품의약품안전처 기본정보, 허가정보 및 낱알식별정보 API에서 지정된 dataset과 page 범위의 데이터를 조회한다. |
|  | 3. 기본정보, 허가정보와 낱알식별정보를 각각 정리하여 최신 동기화 대상으로 준비한다. |
|  | 4. 전체 동기화에서는 세 dataset을 공용 Application Database에 일관되게 반영하고, 일부 dataset 실패 또는 급격한 건수 감소가 감지되면 기존 catalog를 보존한다. |
|  | 5. 동기화된 dataset별 의약품 수와 공용 catalog의 최종 건수를 실행 결과로 출력하며, 일시적 API 실패는 설정된 횟수와 간격으로 재시도한다. |

---

### Use Case Description: UC-24 데이터 생명주기 정리

| Actor Action | System Response |
| --- | --- |
| 1. Backend가 시작되거나 설정된 유지관리 주기가 다시 도달하여 정리 작업이 Trigger된다. |  |
|  | 2. 복약 종료 후 보존기간이 지난 저장 약과 연결되지 않은 복약 완료 기록을 정리한다. |
|  | 3. 만료되었거나 이미 사용된 연결 코드, 보존기간이 지난 건강 추천 cache와 전송 완료 후 보존기간이 지난 보호자 알림 요청을 삭제한다. |
|  | 4. 정리된 데이터가 있는 경우 항목별 삭제 건수를 server log에 기록한다. |

---

### Use Case Description: UC-25 복약정보 직접 등록

| Actor Action | System Response |
| --- | --- |
| 1. 사용자가 약 정보 입력 방식에서 직접 등록을 선택한다. |  |
|  | 2. 선택 사진, 약 이름, 1회 용량과 단위, 복용 시작일·종료일, 시간대 입력 화면을 출력한다. |
| 3. 사용자가 필수 정보를 입력하고 필요한 경우 카메라 또는 사진첩 이미지를 선택한다. |  |
|  | 4. 날짜 범위, 용량, 시간대와 필수값을 기기에서 검증한다. |
| 5. 사용자가 저장을 선택한다. |  |
|  | 6. 선택 이미지를 앱 전용 환자 저장소에 복사하고 공통 저장 복약정보 요청 형식으로 변환한다. |
|  | 7. 기존 복약정보 저장 경계에서 중복 여부를 확인하고 일정과 함께 저장한다. |
|  | 8. 저장 결과를 안내하고 저장 목록과 오늘 복약 일정을 갱신한다. |

---

### Use Case Description: UC-26 다중 낱알 약 일괄 식별

| Actor Action | System Response |
| --- | --- |
|  | 1. 한 장에서 여러 알약 찾기와 알약별 앞면 필수·뒷면 선택 촬영 방식을 함께 출력한다. |
| 2. 사용자가 서로 겹치지 않은 알약을 최대 10개까지 한 장에 촬영하거나 기존 알약별 방식을 선택한다. |  |
|  | 3. 한 장 방식이면 구도를 보존해 이미지를 제한 크기로 정규화하고 별도 다중 식별 API에 한 번 전송한다. |
|  | 4. 서버는 물리적 알약마다 정규화된 경계 상자와 시각 특징을 검증하고, Catalog를 한 번 읽어 각 관찰을 독립 순위화한다. |
|  | 5. 비정상 좌표, 중복에 가까운 경계 상자, 0개 또는 10개 초과 응답은 실패시키며 일정 상태는 변경하지 않는다. |
|  | 6. 원본 사진에 관찰 순서대로 번호를 표시하고 번호별 후보를 출력한다. 기존 알약별 방식은 입력 순서를 보존하며 최대 2개씩 UC-20을 요청한다. |
| 7. 사용자가 각 번호 또는 항목에서 실제 약과 일치하는 후보를 확인한다. |  |
|  | 8. 같은 품목으로 확인된 관찰이 있으면 중복 개수를 표시하고 각각 유지하거나 같은 일정만 하나로 묶을 수 있는 선택 화면을 출력한다. |
| 9. 사용자가 중복 후보를 각각 유지하거나 같은 일정끼리 묶기를 선택한다. |  |
|  | 10. 품목과 사용자가 확인한 시작일·용량·횟수·기간·시간대가 모두 같은 요청만 하나로 묶고, 정리된 후보를 복약 일정 검토 대상으로 전달한다. |

---

### Use Case Description: UC-27 근처 운영 약국 조회

| Actor Action | System Response |
| --- | --- |
| 1. 사용자가 환경설정 실험실에서 근처 운영 약국을 켜고 홈에서 기능을 선택한다. |  |
|  | 2. 전경 위치 권한과 위치 서비스 상태를 확인한다. |
| 3. 사용자가 위치 권한을 허용한다. |  |
|  | 4. 현재 좌표를 인증된 MedBuddy 약국 API로 전송한다. |
|  | 5. Backend가 지역 Catalog를 우선 조회하고, 비었거나 실패하면 공공데이터 API로 보완한 뒤 표시용 필드만 정규화한다. |
|  | 6. 하나의 필터 버튼과 설명형 선택 목록을 제공하고, 선택 조건에 맞는 거리·마감 임박·다음 영업시각·갱신 정보를 담아 기기 즐겨찾기를 우선 배치한 목록을 출력한다. |
| 7. 사용자가 전화 또는 길찾기를 선택하고 설치된 지도 앱, Google 지도 또는 주소 복사 중 하나를 고른다. |  |
|  | 8. 검증된 전화번호 또는 지점명·좌표로 운영체제 앱을 열고, 지도 앱과 브라우저를 모두 열 수 없으면 검증된 주소를 클립보드에 복사한다. |
| 9. 사용자가 짧은 시간 안에 새로고침을 반복한다. |  |
|  | 10. 추가 API 요청 없이 남은 대기시간을 안내하고 제한이 끝난 뒤 다시 허용한다. |
| 11. 사용자가 즐겨찾기 또는 연동 채팅 공유를 선택한다. |  |
|  | 12. 즐겨찾기는 현재 기기에 저장하고, 채팅 공유는 Backend가 Catalog에서 약국 정보를 재검증해 스냅샷으로 저장한다. |

---

### Use Case Description: UC-28 복약 맥락 채팅

| Actor Action | System Response |
| --- | --- |
| 1. 환자 또는 보호자가 환경설정 실험실에서 복약 대화를 켜고 활성 연결의 채팅을 선택한다. |  |
|  | 2. 활성 환자-보호자 연결과 환자의 현재 복용 약 존재 여부를 확인한다. |
|  | 3. 인증된 이력 요청과 WebSocket 연결을 시작하고 읽지 않은 메시지를 표시한다. |
| 4. 사용자가 일반 메시지를 작성하거나 여러 활성 약, 시간대 확인, 약 부족·불편, 약국 공유 중 필요한 구조화 동작을 선택한다. |  |
| 5. 사용자가 전송을 선택한다. |  |
|  | 6. Backend가 연결 권한과 메시지 유형별 역할을 검증하고, 선택한 모든 약의 소유권·복용기간을 확인한 뒤 현재 일정·활성 복용 약·약국 Catalog에서 표시 스냅샷을 다시 만든다. |
|  | 7. 전송자별 client message ID로 중복 요청을 차단하고 메시지 유형과 검증된 구조화 문맥을 저장한다. |
|  | 8. 활성 상대 WebSocket에 새 메시지를 전달하고 상대가 비활성 상태이면 제한된 미리보기와 시간대 이동 정보를 알림으로 보낸다. |
| 9. 상대 사용자가 채팅을 연다. |  |
|  | 10. 참가자별 읽음 시각을 갱신하고 양쪽 화면의 읽음 상태를 동기화한다. |
| 11. 환자가 한 시간대의 약을 모두 복용한다. |  |
|  | 12. 날짜·연동·시간대별 한 번만 자동 완료 메시지를 저장하고 연결 상대에게 전달한다. |
| 13. 사용자가 추천 문구를 선택한다. |  |
|  | 14. 환자 또는 보호자 역할에 맞는 문구를 입력창에 채우며 사용자가 전송 전에 수정할 수 있게 한다. |
| 15. 환자가 시간대 일정 카드를 선택한다. |  |
|  | 16. 오늘의 복약 일정에서 해당 시간대가 보이도록 이동한다. 보호자에게는 같은 카드를 읽기 전용 상태 정보로 제공한다. |

---

### Use Case Description: UC-29 복약 일정 검토 및 보정

| Actor Action | System Response |
| --- | --- |
|  | 1. 처방전 OCR 분석값 또는 사용자가 확인한 낱알 후보를 일정 검토 화면에 표시한다. |
| 2. 사용자가 약별 복용 시작일, 기간, 1일 횟수, 1회 용량과 시간대를 확인한다. |  |
| 3. 잘못된 항목의 편집을 선택하고 값을 수정한다. |  |
|  | 4. 시작일·기간·횟수·시간대 개수와 용량을 검증하고 불완전한 항목을 안내한다. |
| 5. 사용자가 검토 완료를 선택한다. |  |
|  | 6. 사용자 확인값으로 MedicationSchedule을 갱신하여 처방 분석 또는 낱알 저장 흐름에 반환한다. |
