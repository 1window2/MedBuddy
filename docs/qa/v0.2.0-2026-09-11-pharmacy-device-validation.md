# Pharmacy map-first physical validation — 2026-09-11

## Installed baseline and findings

Signed run `34552729578` for `8f08744` succeeded. Artifact `10181670607`
contained APK/AAB files whose checksum manifests both verified. APK SHA-256:
`c308394b1ff15d7a30cd3325fd672b028cb770e78f9dd0daf5f84554612b15db`.
The APK installed in-place without uninstalling or clearing data. App launch
reached the home screen after the update.

On the connected Android phone:

- Naver tiles and 30 pharmacy markers rendered in the expanded map.
- Map height increased from about 588 physical pixels to 1623 on the same
  display, approximately 2.8 times the previous height.
- The full-width list button opened a large panel with existing pharmacy
  cards, hours, address, telephone and directions controls.
- Swiping the list exposed subsequent pharmacies; returning to the map worked.
- Selecting a map marker opened the list and changed map focus; zoom worked.
- Directions opened the chooser; it was dismissed without launching navigation.
- No calls or external navigation were launched.
- At 200% system text size, the pharmacy name wrapped and the bottom toggle
  remained visible. The page heading used an ellipsis; this is not exhaustive
  accessibility sign-off. The original text-size setting was restored.

Physical inspection found the map instruction peeking behind the list header.
Commit `e3d91cf` removes the exposed top gap so the panel covers the map.
Static analysis and Flutter tests passed for this correction; signed build
and final physical recheck are recorded below when complete.

## Loading interpretation

One existing-build refresh became inspectably ready within about seven seconds;
one new-build screen entry within about six seconds. These measurements include
ADB inspection overhead, used different operations/times, and do not isolate
GPS, HTTP or native-map initialization. They are not an optimization benchmark.
Code inspection shows location acquisition precedes one backend search, followed
by map initialization. Widget tests verify that opening/closing the list keeps
the map element and does not issue another pharmacy request.

## Limits

This is one-device portrait validation, not exhaustive accessibility/device
coverage or proof of live pharmacy opening status. No external phone call or
navigation journey was tested. Two-device and public-release gates remain open.

## Requested follow-ups

- `5018446`: lower-right current-location camera button, above attribution.
  It requests a fresh GPS fix, prevents duplicate taps, handles failure, and
  does not call the pharmacy search API.
- `bda99d5`: retain the native map during nonempty-result refreshes, hiding old
  results behind a loading state until the filtered response arrives. List and
  pins use the same response. Regression tests verify that returning from all
  pharmacies to open-now removes nonmatching pins and cards.
- Successful GPS fixes of reported accuracy at most 100 meters can be reused
  in process memory for less than 30 seconds. GPS availability and permissions
  are checked first, clock rollback/expiry clears the cache, and the explicit
  current-location button bypasses reuse. No location is persisted or logged.
  This avoids some repeated GPS waits; it does not guarantee faster cold entry.
- Analysis and Flutter regression tests passed on PR run `34582086404`.
  Full Flutter CI and combined signing run `34582083640` subsequently passed.
  Samsung accidental-touch protection recurred during device access; the user
  was asked to uncover/unlock the phone. Do not infer that the new button or
  final filter behavior has been physically verified from earlier screenshots.

## Final installed artifact

Signed artifact `10192490335`, commit `bda99d5`, installed successfully with
`adb install -r`. Both APK/AAB checksums matched their manifests. APK SHA-256:
`abe2cb64d2f3ff183af87507982191d8e546da176d5a8448e83d4bc46e5fd5db`.
The package update timestamp confirms installation. Final screen inspection
remained on Android system lock/protection UI, not MedBuddy; no authentication
bypass was attempted. Thus final on-screen overlap confirmation, current-location
camera movement, and filtered native-marker checks await an unlocked phone.
Earlier map/list/scroll/zoom/directions evidence applies to the explicitly
identified earlier build, not automatically to this final APK.

## Follow-up — 2026-09-12

Integrated colleague commit `8bcc203` by fast-forward, preserving the map fix.
Focused inspection covered the new photo-mode routing from home and cabinet,
empty front/back slots, cancellation, and explicit candidate/schedule confirmation.
This is not a recognition-accuracy or exhaustive security review.

Physical testing exposed a first-entry readiness bug: assigning the native map
controller did not rebuild the disabled location button. `aaf3ed2` updates state
when the controller becomes ready. Its full CI and protected signed run
`34617752614` passed. Artifact `10271800669` had matching APK/AAB checksum
manifests. APK SHA-256:
`13ff8b477cc43d33a761a94bd9d2a7dfb5e233c2b3f951f20ebbfc78ee588699`.
Installed with `adb install -r`; the app reopened to home.

Verified on that installed build:

- The location button is enabled on first native-map entry, without opening
  the list or refreshing. Camera recentering had also been observed on the
  preceding installed build; no claim of measured GPS accuracy is made.
- All-pharmacy results showed 30 entries; returning to open-now showed three
  entries and visibly removed the additional native-map markers. Overlapping
  markers prevent a screenshot-only exact pin census.
- The list covers the map without the earlier exposed instruction strip.
- Individual-photo mode shows required front and optional back slots. Adding
  an empty second slot does not open the camera; candidate analysis remains
  disabled. Removing that empty slot restores the single-slot state.
- Single-photo mode opens its distinct multiple-pill photo prompt.
- No photos were captured/uploaded, no identification/save action was executed,
  and no calls or navigation journeys were started in these follow-up checks.

An additional overnight issue was observed: changing filters could retain an
implicit reference time from the preceding day. `b775b6d` refreshes implicit
search times while retaining explicitly chosen calendar dates. Date-filtered
cards label status/hours as applying to the search time/date, display that time,
and omit relative closing countdowns that could be mistaken for current status.
The new deterministic midnight regression and Flutter analysis/tests passed;
full CI and protected signed run `34619659416` were still running when this
entry was written. This correction is **not** in the installed `aaf3ed2` APK.

Remaining: install and recheck the search-time correction after signing, real
photo recognition and save-flow validation with approved dummy data, notification
lifecycle gates, and the previously deferred two-device/public-release gates.
An already displayed result is a query snapshot, not a real-time guarantee;
background-resume freshness needs a separate lifecycle regression check.

## Search-time correction installed and rechecked

Both full Flutter CI runs for `b775b6d` passed. Protected signed run
`34619659416` completed successfully; artifact `10272316328` passed the APK/AAB
checksum checks. APK SHA-256:
`c86ed2d2adbf889db14a2010c7e7cd2c559953d3f0f0cc8dbd5a1ffa79bd444c`.
The APK installed successfully in-place and reopened to home.

On this final installed build:

- First map entry again enabled the current-location button without a refresh.
- Late-opening results displayed the current query date/time and the new
  reference-time status/hours labels without visible overlap in portrait.
- Selecting the next calendar day produced a noon reference timestamp. Changing
  from late-opening to weekend/holiday filtering preserved that explicit date
  and timestamp, as intended.
- The overnight implicit-date reset is covered by the deterministic widget
  regression; the device clock was not altered to simulate midnight.

This supersedes the pending-install note above. The broader real-photo,
notification lifecycle, background-resume freshness, two-device and public-release
gates remain open. No main merge or public release was performed.
