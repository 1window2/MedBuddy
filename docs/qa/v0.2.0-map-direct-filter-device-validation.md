# Direct map filters — physical-device validation, 2026-09-14

## Installed revision

- Source: `92e8d8769705287ecc49d40d2a9b28a7a9196846` (PR #108).
- Flutter CI `34755780759`: successful, including analysis, tests and Android
  compile. The initial test-double large-text overflow was corrected to match
  the native map's existing two-line hint limit.
- Protected signed build `34758263910`: successful.
- Artifact `10317254306`; APK SHA-256:
  `f0f9af0070c02386f184b2959184034cb825067ea3a50ee788ffe17390c46411`.
- APK/AAB checksum manifests verified; `adb install -r` returned `Success` on
  the Samsung Android 12 test phone. No uninstall or app-data clearing.

## Observed device checks

All filter/date interactions below were performed without opening the full
pharmacy list:

1. Map toolbar initially displayed the current-opening-hours condition.
2. Selected late-opening mode directly from that toolbar. Old pins disappeared
   while loading; new results and the date control appeared on the same map.
3. Selected weekend/holiday mode, opened the map date picker and chose Sunday,
   2026-09-20. The applied date was `2026-09-20 12:00`; visible pins changed to
   the returned weekend results. The blue device dot remained visible.
4. Selected a named pharmacy pin. Its selected marker and in-map detail sheet
   showed the matching pharmacy, reference-date hours and weekend designation.
   Collapsing the detail sheet worked without opening the full list.
5. Switched back to current-opening-hours while details were selected. The
   selection/details cleared, the date override control disappeared, new results
   loaded and the map stayed open.

The displayed result count was 30 across these searches, but the visible marker
sets differed. This validates UI/search integration, not exhaustive pharmacy
membership or actual real-time opening hours. No pharmacy calls, navigation
launches, favorite changes or medication completion actions were performed.
Location-bearing screenshots remain local, not in Git.

## Session observation and limits

The first post-install launch showed a secure-server-session error on the login
screen. One explicit session-retry action restored the existing home screen
without credential entry, account switching, configuration changes or data
clearing. Its root cause was not established; this run must not be described as
an entirely error-free cold start. Subsequent map interactions succeeded.

The original accidental-touch protection setting remained enabled. No reboot,
network switching, permission changes or two-device tests were performed.
No public release or main-branch merge was made.
