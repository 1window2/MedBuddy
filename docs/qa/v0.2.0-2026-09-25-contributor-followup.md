# September 25 contributor follow-up

## Reviewed baseline

Pulled the 17 commits after `2a63931` through `bbcd278` on the existing
`beta/v0.2.0` branch. The baseline FastAPI, Flutter, and PR checks were green;
PR #108 had no unresolved inline review threads when checked. This review and
automated verification are not a public-release or physical-device sign-off.

| Area | Contributor progress |
| --- | --- |
| Home/widget layout | Adaptive Android widget sizing and launcher reproduction; Home slot swipe indicators; empty slots hidden from previews. |
| Widget reliability | Failed/cancelled background work chains recover; account transitions invalidate stale action tokens, including same-account re-login. |
| Prescription registration | Frequency and selected slots stay consistent; malformed schedule responses fail explicitly; save/refresh retry edge cases are covered. |
| Chat | Read receipts respect route visibility and foreground state, newer read timestamps survive stale responses, and history reading does not force a bottom scroll. |
| Pharmacies | Favorite writes are serialized with failure recovery; fallback search results can be shared through a short-lived server cache. |

The reviewed paths retain the existing boundary/control/entity responsibilities.
No additional blocking defect was confirmed in this review. Contributor emulator
evidence is recorded in the September 23–24 QA reports; it is not new physical
device evidence from this follow-up.

## Follow-up implemented

The September 22 request to put linked-patient guidance below the four Home
action cards was still outstanding. The optional guidance now follows the full
action grid (or narrow-screen list), with spacing only when guidance is present.
Existing eligibility and settings-navigation behavior are unchanged. This is a
boundary-only layout change: no new class, API, persistence, or authorization path.

Four new regressions cover 280/390 logical-pixel viewports and 1x/2x text,
asserting placement below every card and scroll/tap reachability. Existing Home
navigation tests cover linked/unlinked state and switching the schedule source.

## Verification

- Pinned Flutter 3.41.5 with committed dependencies in an isolated frontend copy.
- Focused Home/navigation suite: **130 passed**.
- Full Flutter suite: **1,031 passed**.
- Flutter static analysis: **no issues**.
- Backend suite with a fresh temporary SQLite database: **631 passed, 2 skipped**,
  plus 21 passing subtests. PostgreSQL-specific cases were not run locally.
- The pre-existing local `frontend/pubspec.lock` dependency changes were preserved
  and excluded from this change and its test environment.
- No production deployment, migration, or physical-device test was performed.

## Remaining priorities

1. **Deployment prerequisite:** apply Alembic migration `6d4f8a2c9301` before
   starting the updated backend. It creates `pharmacy_search_cache`; do not deploy
   the new sharing path against the old schema. Follow the existing deployment
   runbook and verify readiness after migration.
2. **Public v0.2.0 release blocker:** restore Firebase App Check / Play Integrity
   enforcement and complete the acceptance criteria in `docs/TODO.md`. The
   direct-install beta exception is not public-release readiness.
3. **Deferred device acceptance:** real Firebase notification actions and chat
   routing, linked-device flows, manufacturer/battery behavior, and real-photo
   prescription recognition remain evidence-gated. Emulator reports do not close
   these physical checks; device testing remains deferred as requested.
4. **Product decision:** keeping five navigation tabs for unlinked users with
   Chat onboarding was proposed in the meeting, not finalized. Confirm that UX
   before changing navigation behavior. Poster planning also remains separate.

Related contributor reports: [widget sizing](v0.2.0-2026-09-23-widget-sizing.md),
[widget logic](v0.2.0-2026-09-24-widget-logic-audit.md),
[action recovery](v0.2.0-2026-09-24-widget-action-recovery.md),
[prescription registration](v0.2.0-2026-09-24-prescription-registration.md), and
[pharmacy/chat](v0.2.0-2026-09-24-pharmacy-chat-audit.md).
