name: medbuddy_frontend
description: A new Flutter project for MedBuddy.
publish_to: 'none'

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # 우리가 추가한 핵심 패키지들
  image_picker: ^1.0.7
  google_mlkit_text_recognition: ^0.15.1
  http: ^1.2.0
  provider: ^6.1.2
  flutter_local_notifications: ^21.0.0
  timezone: ^0.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true